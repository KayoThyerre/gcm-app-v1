# Prisma reads the canonical backend env from ../.env.
# Do not create backend/prisma/.env when backend/.env already exists,
# otherwise Prisma reports duplicated DATABASE_URL definitions.
#
# Use ../.env.example as the single source for required backend variables.
